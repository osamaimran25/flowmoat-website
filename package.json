{
  "name": "flowmoat",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "seo:audit": "npm run build && node scripts/seo-audit.mjs dist"
  },
  "dependencies": {
    "astro": "7.0.9"
  },
  "devDependencies": {
    "cheerio": "^1.2.0"
  }
}
